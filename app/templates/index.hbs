<div class="container">
  <div class="row">
    <div class="col-sm-3 div_box">
      <h4>Navigation</h4>
    </div>
    <div class="col-sm-3 div_box">
      <h4>Jobs</h4>
        <ul>
          {{#each model.categories as |category|}}
            {{category-tile category=category}}
          {{/each}}
        </ul>
    </div>
    <div class="col-sm-3 div_box">
      <h4>Buy / Sell</h4>
        {{#each model.categories as |category|}}
          {{category-tile category=category}}
        {{/each}}
    </div>
  </div>
</div>
